# pnpm 11 reads .npmrc ONLY for registry/auth settings. Every pnpm *behavior*
# setting (engineStrict, minimumReleaseAge, allowBuilds, overrides, catalog, ...)
# lives in pnpm-workspace.yaml and is silently ignored if placed here.
registry=https://registry.npmjs.org/
